export const metadata = {
  title: "Studio Access | Aeethod",
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: {
      index: false,
      follow: false,
    },
  },
};

export default function YamalLayout({ children }) {
  return children;
}
